name: hijriyah_khgt
description: Convert between Hijri Calendar and Gregorian Calendar date support Arabic and Indonesia use KHGT.
version: 0.1.6-1
homepage: https://github.com/acepby/hijriyah_khgt

environment:
  sdk: ">=2.19.6 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
